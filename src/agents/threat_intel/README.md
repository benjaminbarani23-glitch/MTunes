# Agent 3 — Threat Intelligence (planned)

Workers will ingest security feeds, propose `pattern_candidates`, and require admin approval before updating the active pattern database.

MVP ships with seed patterns in `data/patterns/seed.yaml` and the `patterns` table.
